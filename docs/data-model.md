# Modelo de dados — BetIntel

Movido para o repositório do backend: `betintel-backend/docs/data-model.md` (é lá
que o schema é implementado de fato, a partir da Fase 2). Os tipos TypeScript deste
repo (`src/types/`) e os mocks (`src/mocks/`) seguem o mesmo formato descrito lá.
